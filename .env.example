# Required: Get this from GitHub Settings -> Developer Settings -> Personal Access Tokens
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxx

# Optional: ONLY needed for Docker/Production. 
# If left blank, the crawler will save results to 'crawled_repositories.json' locally (Zero-Setup).
# DATABASE_URL=postgres://crawler:password@localhost:5432/github_data
